import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Templatize — Premium Notion Templates",
    template: "%s | Templatize",
  },
  description:
    "Premium Notion templates for AI entrepreneurs, solopreneurs, content creators, and knowledge workers.",
  openGraph: {
    title: "Templatize — Premium Notion Templates",
    description:
      "Premium Notion templates for AI entrepreneurs, solopreneurs, content creators, and knowledge workers.",
    type: "website",
    siteName: "Templatize",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="flex min-h-full flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
